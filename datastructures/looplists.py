letters = ["a", "b", "c", "d", "e"]
# strandart for is used to iterate in loops
for index, letter in enumerate(letters): #if enumerate is added the end results is showing the index of iteration and it becomes a tuple
#in this case the end result prints both the number of index and letter from the list    
    print (index, letter) 